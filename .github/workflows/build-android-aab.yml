name: Build Android AAB (Release)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build (e.g. develop, main, feat/xyz)'
        required: true
        default: 'develop'
      environment:
        description: 'Build environment'
        required: true
        default: 'production'
        type: choice
        options:
          - development
          - preview
          - production

jobs:
  build-android:
    name: Build AAB (${{ github.event.inputs.environment || 'production' }})
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      # ── 1. Checkout coco-patient-mobile source ─────────────────────────────
      - name: Checkout coco-patient-mobile
        uses: actions/checkout@v4
        with:
          repository: coco-staging/Coco-patient-mobile
          ref: ${{ github.event.inputs.branch }}
          path: app
          token: ${{ secrets.COCO_PAT }}

      # ── 2. Setup tools ─────────────────────────────────────────────────────
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.4'
          cache: 'yarn'
          cache-dependency-path: app/yarn.lock

      # ── 3. Cache Gradle ────────────────────────────────────────────────────
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('app/android/gradle/wrapper/gradle-wrapper.properties', 'app/yarn.lock') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      # ── 4. Install dependencies ────────────────────────────────────────────
      # postinstall / eas-build-post-install already strips the DocuSign AAR
      # (scripts/strip-docusign-aar.sh) — no manual patch step needed anymore.
      - name: Install dependencies
        working-directory: app
        run: yarn install --frozen-lockfile

      # ── 5. Write .env.<environment> file from secrets ──────────────────────
      # scripts/set-{dev,preview,prod}-env.sh (invoked by the yarn eas:local
      # scripts below) source this exact filename — eas build --local doesn't
      # reliably pull EAS-hosted environment variables on its own
      # (expo/eas-cli#2195), so the file has to exist on disk first.
      - name: Write .env file
        working-directory: app
        env:
          ENV_API_URL: ${{ secrets.EXPO_PUBLIC_API_URL }}
          ENV_ENVIRONMENT: ${{ github.event.inputs.environment || 'production' }}
          ENV_SENTRY_URL: ${{ secrets.EXPO_PUBLIC_SENTRY_URL }}
          ENV_SENTRY_DSN: ${{ secrets.EXPO_SENTRY_URL }}
          ENV_SENTRY_PROJECT: ${{ secrets.EXPO_SENTRY_PROJECT_NAME }}
          ENV_SENTRY_ORG: ${{ secrets.EXPO_SENTRY_ORGANIZATION }}
          ENV_SENTRY_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          ENV_API_PIN: ${{ secrets.EXPO_PUBLIC_API_PIN_SHA256 }}
          ENV_API_PIN_EXPIRY: ${{ secrets.EXPO_PUBLIC_API_PIN_EXPIRES_AT }}
          ENV_NAME: ${{ github.event.inputs.environment || 'production' }}
        run: |
          {
            echo "EXPO_PUBLIC_API_URL=${ENV_API_URL}"
            echo "EXPO_PUBLIC_ENVIRONMENT=${ENV_ENVIRONMENT}"
            echo "EXPO_PUBLIC_SENTRY_URL=${ENV_SENTRY_URL}"
            echo "EXPO_SENTRY_URL=${ENV_SENTRY_DSN}"
            echo "EXPO_SENTRY_PROJECT_NAME=${ENV_SENTRY_PROJECT}"
            echo "EXPO_SENTRY_ORGANIZATION=${ENV_SENTRY_ORG}"
            echo "SENTRY_AUTH_TOKEN=${ENV_SENTRY_TOKEN}"
            echo "EXPO_PUBLIC_API_PIN_SHA256=${ENV_API_PIN}"
            echo "EXPO_PUBLIC_API_PIN_EXPIRES_AT=${ENV_API_PIN_EXPIRY}"
          } > ".env.${ENV_NAME}"

      # ── 6. Install eas-cli ──────────────────────────────────────────────────
      - name: Install eas-cli
        run: npm install -g eas-cli@16.18.1 --silent

      # ── 7. Build AAB via EAS local build ────────────────────────────────────
      # Signing is handled by EAS itself (eas.json → credentialsSource: remote
      # + --freeze-credentials pulls the registered upload keystore from EAS'
      # own credential store — no keystore secret / credentials.json needed).
      # Replaces the old manual decode-keystore / write-credentials.json /
      # expo-prebuild / DocuSign-AAR-patch / JVM-target-patch / Metaspace-bump
      # / gradlew steps, all of which the yarn script + app's own EAS/plugin
      # config now cover.
      - name: Build AAB
        working-directory: app
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          BUILD_ENV: ${{ github.event.inputs.environment || 'production' }}
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "ERROR: EXPO_TOKEN secret not set — add the EAS robot-user access token as a repo secret named EXPO_TOKEN." >&2
            exit 1
          fi

          case "$BUILD_ENV" in
            development) SCRIPT="android:eas:local:dev" ;;
            preview)     SCRIPT="android:eas:local:preview" ;;
            production)  SCRIPT="android:eas:local:prod" ;;
            *) echo "ERROR: unknown environment '$BUILD_ENV'" >&2; exit 1 ;;
          esac

          yarn "$SCRIPT" --output app-release.aab

      # ── 8. Get source commit SHA ────────────────────────────────────────────
      - name: Get source commit SHA
        working-directory: app
        run: echo "SOURCE_SHA=$(git rev-parse HEAD)" >> $GITHUB_ENV

      # ── 9. Create tag + GitHub Release ──────────────────────────────────────
      - name: Create Release
        env:
          GH_TOKEN: ${{ secrets.COCO_PAT }}
        run: |
          TAG="build-aab-${{ github.event.inputs.environment || 'production' }}-$(date +%Y%m%d)-${SOURCE_SHA:0:7}"
          NOTES="Branch: ${{ github.event.inputs.branch }}
          Environment: ${{ github.event.inputs.environment || 'production' }}
          Commit: ${SOURCE_SHA}"
          gh release create "$TAG" \
            --repo coco-staging/Coco-patient-mobile \
            --title "AAB ${{ github.event.inputs.environment || 'production' }} $(date '+%Y-%m-%d')" \
            --notes "$NOTES" \
            app/app-release.aab
      # `eas build --local` already registers the build with EAS itself
      # (confirmed on a real run: "Uploading your app archive to EAS...
      # Uploaded to EAS"), so no separate `eas upload` step is needed here —
      # calling it again hits the account's local-builds quota / duplicate
      # registration and fails outright.
